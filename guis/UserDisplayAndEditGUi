package gui;

import javax.swing.*;
import objects.Person;
import BreezySwing.*;

@SuppressWarnings({"serial", "unused"})
public class UserDisplayAndEditGUI extends SearchGUI {
	
	public UserDisplayAndEditGUI(JFrame frm, Person[] people) {
		super(frm, people);
		setSize(400, 300);
		setResizable(false);
		setTitle("Person Display");
	}
	
	public void resetFrame() {
		
	}
}
